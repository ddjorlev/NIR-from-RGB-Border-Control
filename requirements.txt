numpy>=1.25
Pillow>=9.0
opencv-python>=4.7
tqdm>=4.64
tensorboard>=2.15
pandas>=2.0
scikit-image>=0.20
matplotlib>=3.7
# torchvision  # installed together with torch (see install command)
# torchaudio   # installed together with torch (see install command)
einops>=0.6
albumentations>=1.3
scipy>=1.10
python-multipart>=0.0.5
typing-extensions>=4.5
wandb>=0.15  # optional, remove if you don't use Weights & Biases
lpips>=0.1.4  # perceptual loss / metric (optional)